<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="cal_button" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/blueLight</item>
        <item name="colorButtonNormal">@color/blueDark</item>
    </style>

    <style name="save_button" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/redLight</item>
        <item name="colorButtonNormal">@color/redDark</item>
    </style>

</resources>
